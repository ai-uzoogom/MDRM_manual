# 🛠️ Agent 포트 변경 방법

!!! info "개요"
    Agent의 기본 통신 포트(20080)가 다른 서비스와 충돌하거나 고객사 보안 정책상 변경이 필요한 경우의 조치 방법입니다.

---

## **1. Agent 설정 파일 수정**

Agent 설치 경로 내의 설정 파일을 직접 수정해야 합니다.

1.  **설정 파일 위치**: 
    *   Linux/Unix: `/opt/mdrm_agent/conf/agent.properties` (기본 경로)
    *   Windows: `C:\Program Files\MDRM_Agent\conf\agent.properties`
2.  **수정 항목**: `server.port` 값을 원하는 포트 번호로 변경합니다.
    ```properties
    # 기본값: 20080
    server.port=20090
    ```

---

## **2. 서비스 재시작**

설정 반영을 위해 Agent 서비스를 재시작합니다.

*   **Linux**:
    ```bash
    systemctl restart mdrm_agent
    ```
*   **Windows**:
    *   `services.msc` 실행 → `MDRM Agent` 서비스 재시작

---

## **3. MDRM 서버 설정 업데이트**

Agent 포트가 변경되면 자동 연결이 끊어집니다. MDRM 웹 콘솔에서 해당 시스템의 정보를 수동으로 업데이트해야 합니다.

1.  **시스템 관리** 메뉴 접속
2.  대상 서버 선택 후 **수정** 클릭
3.  **포트 정보**를 변경된 포트(예: 20090)로 업데이트
4.  **저장** 및 **연결 테스트** 수행

---

!!! warning "방화벽 확인 필수"
    포트 변경 후 반드시 네트워크 방화벽(L4, OS 방화벽 등)에서 변경된 포트가 개방되어 있는지 확인하십시오.
